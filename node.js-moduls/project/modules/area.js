function areaCircle(radius) {
    return Math.PI * radius * radius;
}

function areaRectangle(lenght, width) {
    return lenth * width;
}

module.exports = {
    areaCircle,
    areaRectangle
};